specVersion: "v1"
serviceId: "bitetailor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bitetailor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
